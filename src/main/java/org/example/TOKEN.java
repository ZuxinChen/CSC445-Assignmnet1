package org.example;

// token types
public enum TOKEN {
    VAR,
    WRITE,
    INIT,
    IF,
    THEN,
    ENDIF,
    WHILE,
    DO,
    ENDWHILE,
    CALCULATE,
    PLUS,
    EQUALS,
    NOTEQUALS,
    SCANEOF,
    ID,
    INTLITERAL,
    STRING,
    MINUS,
    AND,
    OR,
    LEFT_BRACKET,
    RIGHT_BRACKET,
    UNDEFIND

}
